The code allows you to Paste your code in the boxes and click "scan code" to check the code for simularites.

Unfortaintly I found out the code will not actually read the files to the system too late into the deadline. so this is what I have.